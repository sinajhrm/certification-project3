/**
 *
 * @typedef {object} Subtask
 * @property {string} id
 * @property {string} title A positive number with floating point
 * @property {Date} dueDate Due date of the subtask
 * @property {boolean} isCompleted A positive number with floating point
 * @property {string} priority A positive number with floating point
 *
 * @typedef {object} Task
 * @property {string} id
 * @property {string} title A positive number with floating point
 * @property {Subtask[]} subtasks
 *
 */
export const Types = {}
